#include <stdio.h>
/**
 * main - prints the lowercase alphabet in reverse
 *
 * Return: Always (0) success
 */
int main(void)
{
	int rettel;

	rettel = 122;
	while (rettel > 96)
	{
		putchar(rettel);
		rettel--;
	}
	putchar(10);
	return (0);
}
