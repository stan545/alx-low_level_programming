#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the alphabet in lowercase
 * and then in uppercase
 *
 * Return: Always 0 (succes)
 */

int main(void)
{
	int i = 97;

	while i < 123;
	{
		putchar(i)
		i++;
	}
	putchar(10);

	return (0);
}
