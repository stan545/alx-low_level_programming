#include <stdio.h>
#include "main.h"

/**
 * add - A function that sums two integers
 * @c: first integer input
 * @a: second integer input
 * Return: Always return sum (success)
 */
int add(int c, int a)
{
	int sum;

	sum = c + a;
	return (sum);
}
