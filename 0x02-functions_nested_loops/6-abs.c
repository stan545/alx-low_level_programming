#include <unistd.h>
#include <stdlib.h>
#include "main.h"
/**
  * _abs -  computes the absolute value of an integer.
  *
  * @n: the number whose absolute value is computed
  *
  * Return: returns 0
  */
int _abs(int n)
{
	return (abs(n));
	return (0);
}
